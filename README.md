# NewsApi

NewsAPI client for Android

## Installation

App can be installed with gradle wrapper. Go to project's main catalog and enter command to build and install app on your connected device or emulator.

```
./gradlew installDebug
```

After successful install, app can be found on your device.

## Testing

App has a suite of Unit Tests. To run them, simply enter command

```
./gradlew test
```